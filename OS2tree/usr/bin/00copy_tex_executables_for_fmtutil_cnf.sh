
#
# copy executables according to fmtutil.cnf
if [ ! mf = mf ] ; then
	if [ -f mf.exe ] ; then
		cp -vp mf.exe mf.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! mpost = mpost ] ; then
	if [ -f mpost.exe ] ; then
		cp -vp mpost.exe mpost.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! metafun = mpost ] ; then
	if [ -f mpost.exe ] ; then
		cp -vp mpost.exe metafun.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! ptex = ptex ] ; then
	if [ -f ptex.exe ] ; then
		cp -vp ptex.exe ptex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! platex = ptex ] ; then
	if [ -f ptex.exe ] ; then
		cp -vp ptex.exe platex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! ptexinfo = ptex ] ; then
	if [ -f ptex.exe ] ; then
		cp -vp ptex.exe ptexinfo.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! ptexinfo-ps = ptex ] ; then
	if [ -f ptex.exe ] ; then
		cp -vp ptex.exe ptexinfo-ps.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! texinfo-ps = tex ] ; then
	if [ -f tex.exe ] ; then
		cp -vp tex.exe texinfo-ps.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! texinfo = tex ] ; then
	if [ -f tex.exe ] ; then
		cp -vp tex.exe texinfo.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdftexinfo = pdftex ] ; then
	if [ -f pdftex.exe ] ; then
		cp -vp pdftex.exe pdftexinfo.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdftexinfo-ps = pdftex ] ; then
	if [ -f pdftex.exe ] ; then
		cp -vp pdftex.exe pdftexinfo-ps.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! enctex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe enctex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! enclatex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe enclatex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! tex = tex ] ; then
	if [ -f tex.exe ] ; then
		cp -vp tex.exe tex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! latex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe latex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! aleph = aleph ] ; then
	if [ -f aleph.exe ] ; then
		cp -vp aleph.exe aleph.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! etex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe etex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdftex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe pdftex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdflatex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe pdflatex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdfetex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe pdfetex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! omega = omega ] ; then
	if [ -f omega.exe ] ; then
		cp -vp omega.exe omega.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! lambda = omega ] ; then
	if [ -f omega.exe ] ; then
		cp -vp omega.exe lambda.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! lamed = aleph ] ; then
	if [ -f aleph.exe ] ; then
		cp -vp aleph.exe lamed.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdfxtex = pdfxtex ] ; then
	if [ -f pdfxtex.exe ] ; then
		cp -vp pdfxtex.exe pdfxtex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! pdfxlatex = pdfxtex ] ; then
	if [ -f pdfxtex.exe ] ; then
		cp -vp pdfxtex.exe pdfxlatex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! cont-cz = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe cont-cz.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! cont-en = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe cont-en.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! utf8mex = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe utf8mex.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! eplain = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe eplain.exe
	fi
fi

#
# copy executables according to fmtutil.cnf
if [ ! mptopdf = pdfetex ] ; then
	if [ -f pdfetex.exe ] ; then
		cp -vp pdfetex.exe mptopdf.exe
	fi
fi
